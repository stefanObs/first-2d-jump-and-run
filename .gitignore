# Godot 4 generated files
.godot/
*.translation

# OS / editor
.DS_Store
*~
*.swp
.vscode/
.idea/

# Exports and local tooling
export_presets.cfg
*.pck
*.zip
*.apk
*.aab
*.exe
*.x86_64
*.app
dist/
build/

# Player save data (campaign + custom trails) — kept local, never committed
savegames/

# Windows shortcuts / locally built play launcher
*.lnk
/Play Cowboy Trail.exe

# Keep the bundled Windows engine so the game runs with no install.
# (The .exe is unpacked from this zip on first launch and stays ignored.)
!godot/Godot_v4.4.1-stable_win64.exe.zip

# Godot import sidecars for raw assets
*.import
