class_name TiedBanditOverlay
extends Node2D

## Marker for a captured bandit. Rope coils live in the tied floor sprite.


func _ready() -> void:
	# Keep a named child so tests and restores can detect the tied state.
	pass
